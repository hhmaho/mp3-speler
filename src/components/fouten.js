// for (var currentsong = 0; currentsong < this.props.songs.length; currentsong++) {
//     var previous = this.props.songs[currentsong - 1];
//     // var current = this.props.songs[currentsong];
//     // var next = this.props.songs[currentsong + 1];
// }


 render() {
        // for (var currentsong = 0; currentsong < this.props.songs.length; currentsong++) {
        //     var previous = this.props.songs[currentsong - 1];
        // }
        return (
            <div>
                {/* <button onClick={() => this.setState({ previous: this.state.currentsong })}>
                <button onClick={previous}>&lt;&lt;</button> */}